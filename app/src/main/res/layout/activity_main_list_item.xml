<?xml version="1.0" encoding="utf-8" ?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingEnd="@dimen/activity_main_padding"
    android:paddingStart="0dip"
    >

    <ImageView
        android:id="@+id/activity_main_list_item_picture"
        android:layout_width="@dimen/activity_main_list_item_picture_size"
        android:layout_height="@dimen/activity_main_list_item_picture_size"
        android:layout_alignParentEnd="true"
        android:contentDescription="@string/loading_prompt"
        />
    <TextView
        android:id="@+id/activity_main_list_item_title"
        android:layout_width="match_parent"
        android:layout_height="@dimen/activity_main_list_item_picture_size"
        android:layout_toStartOf="@id/activity_main_list_item_picture"
        android:gravity="center_vertical"
        android:maxLines="2"
        android:ellipsize="end"
        />

</RelativeLayout>