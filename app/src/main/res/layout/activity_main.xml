<?xml version="1.0" encoding="utf-8" ?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/activity_main_padding"
    android:orientation="vertical"
    >

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        >

        <EditText
            android:id="@+id/activity_main_search_box"
            android:layout_width="match_parent"
            android:layout_height="50dip"
            android:paddingRight="30dip"
            android:hint="@string/activity_main_search_box_hint"
            android:inputType="text"
            android:imeOptions="actionSearch"
            />
        <ImageView
            android:id="@+id/activity_main_clear_search_button"
            android:layout_width="25dip"
            android:layout_height="25dip"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:src="@drawable/btn_clear"
            />

    </RelativeLayout>
    <ListView
        android:id="@+id/activity_main_content_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingRight="@dimen/activity_main_padding_for_scrollbar"
        android:footerDividersEnabled="false"
        android:fadingEdge="none"
        android:fadingEdgeLength="0dip"
        android:scrollingCache="false"
        android:cacheColorHint="#00000000"
        android:verticalScrollbarPosition="right"
        />

</LinearLayout>